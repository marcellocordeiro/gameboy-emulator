[workspace]
resolver = "2"
members = [
  "gb-core",
  "gb-core-c",
  "gb-core-wasm",
  "gb-opcode-info",
  "gb-ui-native",
]

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true

# Reminder to use `cargo clippy`.
