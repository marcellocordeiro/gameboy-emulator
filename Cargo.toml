[workspace]
resolver = "2"
members = [
  "core/gb-core",
  "core/gb-core-c",
  "core/gb-core-wasm",
  "core/gb-opcode-info",
  "platform/eframe-rust",
  "platform/libretro",
  "platform/sdl2-rust",
]

# Reminder to use `cargo clippy`.

[workspace.dependencies]
gb-core = { path = "./core/gb-core" }
